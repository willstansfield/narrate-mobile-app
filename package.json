{
  "name": "narrate-mobile",
  "version": "1.0.0",
  "description": "Narrate billing app — Capacitor wrapper for the Narrate PWA",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
